// 课程章节完成状态枚举
export enum SectionStatus {
  COMPLETED = "COMPLETED",
  STARTED = "STARTED",
  NOT_STARTED = "NOT_STARTED"
}

// 证书请求状态枚举
export enum CertificateStatus {
  PENDING = "PENDING",
  APPROVED = "APPROVED",
  REJECTED = "REJECTED",
  ISSUED = "ISSUED"
}

// 其他类型可以在这里添加 